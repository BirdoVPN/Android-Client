package app.birdo.vpn.utils

/**
 * Re-export from shared KMP module.
 * Preserves the existing import path for the Android codebase.
 */
typealias InputValidator = app.birdo.vpn.shared.util.InputValidator
